name: Deploy Branch

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          script: |
            bash /usr/local/bin/deploy_branch.sh ${{ github.ref_name }} https://github.com/${{ github.repository }}.git
